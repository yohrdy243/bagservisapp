import { Text } from "native-base";
import { View } from "react-native";

const AppSettings = () => {
  return (
    <View>
      <Text bold>This is the App Settings</Text>
    </View>
  );
};  

export default AppSettings;
